let i = 1; // Initialize the variable i to 1
while(i <= 5) // Condition Continue the loop as long as i is less than or equal to 5
{
    console.log(i); // Work Print the current value of i to the console
    i++;    // Update Increment i by 1 in each iteration to eventually break the loop
}